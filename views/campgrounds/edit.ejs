<% layout('/layouts/boilerplate') %>
<form action="/campgrounds/<%=campground.id%>/?_method=PUT" method="POST">
  <label for="location">Campground Location</label>
  <select name="campground[location]">
    <% for (location of locations) { %>
    <option><%= location.city %>, <%= location.state %></option>
    <% } %>
  </select>
  <label for="title">Campground Name:</label>
  <input
    id="title"
    name="campground[title]"
    type="text"
    value="<%= campground.title %>"
  />
  <button>Submit</button>
</form>

<a href="/campgrounds/<%=campground.id%>"><button>Cancel</button></a>
<a href="/campgrounds"><button>All Campgrounds</button></a>
